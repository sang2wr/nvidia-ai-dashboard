# NVIDIA NIM 카탈로그 재조사 — 2026-09-04 (중간 저장, 미완)

키: `nvidia_ai_dashboard/.streamlit/secrets.toml`. 조회/시험 스크립트를 이 폴더에 저장해둠.
- `fetch_catalog.py` — `/v1/models` 전체 목록을 뜬다 (결과: `카탈로그_20260904.txt`, 81개)
- `probe_models.py` — 모델별로 실제 chat 호출해 200/404/410 판별 (한국어 단답 테스트 겸함)
- 실행: `py -3.12 fetch_catalog.py` / `py -3.12 probe_models.py`
- **`requests` 쓰지 말 것 — 이 PC에선 멈춤. `urllib.request`로 짜여 있음**

## ★ 가장 중요한 발견 — 대시보드가 사실상 고장 상태
`nvidia-ai-dashboard.streamlit.app`에 등록된 모델 7개 중 **6개가 410 Gone("has reached its end of life")**.
지금 사이트에서 "빠른 일반대화" 빼고 아무 카테고리나 고르면 오류가 날 것으로 보임(라이브 화면 확인은 아직 안 함).

| 대시보드 등록 모델 | 카테고리 | 상태 |
|---|---|---|
| meta/llama-3.3-70b-instruct | 문서 | **410 Gone** |
| meta/llama-3.1-70b-instruct | 문서 | **410 Gone** |
| openai/gpt-oss-120b | 코드 | **410 Gone** |
| openai/gpt-oss-20b | 코드 | ✅ 정상(3.0s) — 단, 사고과정이 reasoning_content로 나옴 |
| nvidia/llama-3.3-nemotron-super-49b-v1.5 | 추론 | **410 Gone** |
| meta/llama-3.1-8b-instruct | 빠른대화 | **410 Gone** |
| z-ai/glm-5.2 | 빠른대화 | **410 Gone** |

`meta/llama-3.1-70b-instruct`는 [[technique_nvidia_reasoning_model_ignores_instructions]]에서
"단답이 필요하면 이걸 쓰라"고 정해둔 모델인데 **이것도 죽음** → 그 기법 메모리도 갱신 필요.
이 모델을 참조하는 다른 프로젝트(제안서 PPT 생성기 등)도 같이 깨졌을 가능성 있음 — **미확인, 다음에 확인할 것**.

## 실호출로 확인된 정상 모델 (2026-09-04)
한국어 단답 테스트(`'서울'이라고만 답하세요. 대한민국의 수도는?`) 결과:

| 모델 | 응답 | 지시 준수 | 비고 |
|---|---|---|---|
| `nvidia/nemotron-3-nano-omni-30b-a3b-reasoning` | 1.4s | ✅ '서울' | 빠르고 깔끔. omni(멀티모달?) — **확인 필요** |
| `minimaxai/minimax-m3` | 0.9s | ✅ '서울' | 가장 빠른 축 |
| `poolside/laguna-xs-2.1` | 0.9s | ✅ '서울' | 코드용 모델로 추정 — **확인 필요** |
| `nvidia/ising-calibration-1.5-31b` | 0.6s | ✅ '서울' | 정체 불명 — **확인 필요** |
| `google/gemma-4-31b-it` | 6.5s | ✅ '서울' | 느림 |
| `nvidia/nemotron-3-ultra-550b-a55b` | 6.6s | ❌ 사고과정 출력 | 최상위 추론모델, 장문 분석용 |
| `nvidia/nemotron-3.5-lightning-30b-a3b` | 1.2s | ❌ 사고과정 출력 | 이름은 lightning인데 thinking을 뱉음 |
| `meta/muse-glimmer-30b` | 0.5s | ❌ 프롬프트 반복 | 쓸만한지 의문 |
| `google/diffusiongemma-26b-a4b-it` | 0.5s | ❌ 빈 응답 | diffusion LM, 별도 사용법 필요할 듯 |
| `openai/gpt-oss-20b` | 3.0s | ❌ 사고과정 출력 | 기존 코드 카테고리 모델 |
| `nvidia/riva-translate-4b-instruct-v2` | 1.0s | (번역기) | 입력을 **영어로 번역**해서 돌려줌 — 채팅 아님, 번역 전용 |

## 404 (카탈로그엔 있으나 이 계정에서 접근 불가)
`nvidia/nemotron-nano-3-30b-a3b`, `moonshotai/kimi-k2.6`, `google/gemma-3-12b-it`,
`nvidia/llama-3.1-nemotron-70b-instruct`, `nvidia/llama-3.1-nemotron-ultra-253b-v1`,
`writer/palmyra-fin-70b-32k`, `writer/palmyra-creative-122b`, `nvidia/cosmos-reason2-8b`

## 재시험 필요 (90초 타임아웃 — 콜드스타트 가능성, 죽었다고 단정 못 함)
`deepseek-ai/deepseek-v4-pro-0813`, `deepseek-ai/deepseek-v4-flash-0731`, `moonshotai/kimi-k3`,
`mistralai/mistral-nemotron`, `meta/llama-3.2-90b-vision-instruct`
→ **타임아웃을 180초로 늘리고 2회 재시도해서 다시 판정할 것**

## 특기: nemotron-parse (문서 판독 전용)
`nvidia/nemotron-parse`는 400을 반환하며 **"Content cannot be a plain string. The model does not
support text input"** — 즉 **이미지/문서 입력 전용 파서**. 서류 검토(F15 출석부·F26 결과보고 등)
OCR·표 추출에 직접 쓸 수 있는 후보라 **다음에 이미지 입력 형식으로 반드시 시험해볼 것**.
`nvidia/nemotron-3-super-120b-a12b`는 503(일시 과부하)이라 재시험 대상.

## 아직 안 본 것 (다음 작업)
1. 위 재시험 5종 + nemotron-parse 이미지 입력 시험 + super-120b 재시험
2. 미시험 카탈로그 모델: 임베딩 계열(`nvidia/nemotron-3-embed-1b`, `llama-3.2-nv-embedqa-1b-v1`,
   `nvclip`, `llama-nemotron-embed-vl-1b-v2`), 안전필터(`nemoguard`/`nemotron-3.5-content-safety`),
   비전(`llama-3.2-11b-vision`, `phi-3-vision`, `neva-22b`, `vila`, `kosmos-2`, `deplot`, `fuyu-8b`),
   `nvidia/ai-synthetic-video-detector`, `writer/palmyra-med-70b`
3. 이미지 생성(`flux.2-klein-4b`)은 별도 엔드포인트(`ai.api.nvidia.com`)라 이번 목록에 없음 — 생사 미확인
4. **대시보드 app.py 모델 교체 + 재배포** (410 6종 → 정상 모델로)
5. 기존 `NVIDIA_모델_전수조사.xlsx`와 대조 (아직 안 열어봄)
6. 사용자 프로젝트별 활용 제안 정리 (서류 OCR / 주식 / 쇼츠 / 제안서 PPT)

# NVIDIA NIM 카탈로그 재조사 — 2026-09-05 (완료)

2026-09-04 중간 조사(`조사_20260904_중간.md`)를 이어받아 마무리하고 대시보드를 정상화했다.

## 결론 1 — 카탈로그(/v1/models)는 신뢰할 수 없다

목록엔 81개가 나오지만 **구형 모델은 실제 호출 시 대부분 404 "Function not found"** 다.
2026-09-05에 미시험 26종을 전수 호출한 결과 **22종이 404**:

> mistral 전 계열(large/large-2/7b-v0.3/mixtral-8x22b/codestral/nemo-12b), gemma-2b·gemma-3-4b·codegemma-7b,
> granite-3.0-8b·granite-34b-code, yi-large, dbrx-instruct, jamba-1.5-large, codellama-70b, llama2-70b,
> zamba2-7b, phi-3.5-moe, palmyra-med-70b, deepseek-coder-6.7b, sea-lion-7b, starcoder2-15b,
> nemotron-4-340b, llama-3.1-nemotron-51b, llama3-chatqa-1.5-70b, mistral-nemo-minitron-8b

**목록만 보고 고르면 안 되고 반드시 실호출로 확인할 것.** (`probe_models.py` / `probe2.py`)

## 결론 2 — 대시보드는 고장 상태였고, 고쳤다

등록 모델 7개 중 6개가 410 Gone. 여기에 더해 **어제 못 봤던 것**:
`POSTER_TEXT_MODEL = "meta/llama-3.1-70b-instruct"` 단일 지정이라 **포스터 문구·영문 프롬프트 생성이 통째로 죽어 있었다.**

고침(커밋 `407530a`):
- 채팅 4개 카테고리 모델 전면 교체
- `POSTER_TEXT_MODELS` 폴백 목록 + `call_chat_fallback()` — 한 모델이 죽어도 기능 유지
- 추론모델 경고를 `REASONING_MODELS` 접두사 목록으로 일반화
- 불안정 모델 `poolside/laguna-xs-2.1` 제외(503 잦음, 응답 22~39초로 튐)

## 2026-09-05 기준 살아있는 모델 (카테고리 과제까지 통과)

| 모델 | 문서 | 코드 | 추론 | 대화 | 메모 |
|---|---|---|---|---|---|
| `nvidia/ising-calibration-1.5-31b` | 1.4s | 0.7s | 0.4s | 3.3s | 전 영역 최고속. 자기소개로는 "Gemma 4 기반 캘리브레이션 해석 모델"이라 함 |
| `google/gemma-4-31b-it` | 6.4s | 4.1s | 6.5s | 3.5s | 느리지만 가장 안정적 |
| `minimaxai/minimax-m3` | 1.3s | 0.6s | (429) | (429) | 요약 정확. 연속 호출 시 429 |
| `nvidia/nemotron-3-super-120b-a12b` | 1.1s | 1.5s | 1.2s | 사고과정 누출 | 빠른 대형 모델 |
| `nvidia/nemotron-3-nano-omni-30b-a3b-reasoning` | 1.7s | 3.1s | 1.4s | 3.6s | 가볍고 답이 짧음 |
| `nvidia/nemotron-3-ultra-550b-a55b` | (503) | 5.5s | 7.6s | 3.3s | 최상위 추론 |
| `openai/gpt-oss-20b` | reasoning만 | 1.0s | 9.4s | reasoning만 | **코드 전용으로만 쓸 것** |
| `poolside/laguna-xs-2.1` | 22s | 0.6s | 1.5s | 39s | 503 잦음 — 대시보드에서 제외 |
| `mistralai/mistral-nemotron` | 500 | 500 | 93.6s | 0.6s | 단답은 0.2s인데 실과제에서 불안정 → 부적합 |
| `moonshotai/kimi-k3` | — | — | — | — | 105초, 사고과정만 → 부적합 |

기타 생존: `nvidia/llama-3.1-nemoguard-8b-content-safety`·`nvidia/nemotron-3.5-content-safety`(안전 분류기,
`{"User Safety": "safe"}` 반환), `nvidia/riva-translate-4b-instruct-v1.1/v2`(번역 전용 — 입력을 영어로 바꿔 돌려줌).

**여전히 타임아웃(180초·2회에도)**: `deepseek-v4-pro-0813`, `deepseek-v4-flash-0731`,
`llama-3.2-90b-vision-instruct`, `meta/llama-guard-4-12b` → 사실상 사용 불가로 간주.

## 이미지 생성 — 정상

`ai.api.nvidia.com/v1/genai/black-forest-labs/flux.2-klein-4b` 생존 확인(1.7초, 269KB).
steps 최대 4, `cfg_scale`/`mode` 미지원(422), 응답은 JPEG.

## 다른 프로젝트 오염 검사 (어제 미확인 항목)

죽은 모델 ID를 참조하는 곳을 전 프로젝트에서 검색한 결과 **`shorts_night` 한 곳**:
`config.yaml`의 `chat_models` 폴백에 `deepseek-v4-flash-0731`(타임아웃)·`gpt-oss-120b`(410)가 남아 있어
실질적으로 gemma-4 하나로 버티는 상태였다. → 검증된 4종으로 교체.
순서는 **minimax-m3 → gemma-4 → nano-omni → ising**: 이 작업은 "JSON 배열만 출력"이라 형식 준수가 관건인데
ising은 설명을 덧붙여 파싱이 깨졌고 minimax-m3가 한 번에 통과했다. `reasoning_content` 폴백도 추가.

## 라이브 검증 (2026-09-05)

`https://nvidia-ai-dashboard.streamlit.app` — 푸시 후 재빌드 완료 확인.
- 모델 드롭다운에 `nvidia/ising-calibration-1.5-31b` 노출(죽은 모델 흔적 없음)
- 채팅 실제 응답 정상
- **포스터 1단계(영문 프롬프트 생성) 실제 동작 확인** — 죽어 있던 기능이 살아남
- 앱은 `/~/+/` iframe 안에서 렌더됨 → 브라우저 자동화 시 이 URL로 직접 접근해야 DOM이 잡힘

## 문서 파서 `nvidia/nemotron-parse` — 시험 완료, 표 추출은 실용 수준

**호출 형식이 특이하다**: 텍스트를 같이 보내면 400("does not support text input"). **이미지만** 보내야 하고,
응답은 `message.content`가 아니라 **`tool_calls[0].function.arguments`(JSON 문자열)** 에 담긴다.

```python
payload = {"model": "nvidia/nemotron-parse", "max_tokens": 4096,
           "messages": [{"role": "user", "content": '<img src="data:image/png;base64,...">'}],
           "tools": [{"type": "function", "function": {"name": "markdown_no_bbox"}}]}
args = resp["choices"][0]["message"]["tool_calls"][0]["function"]["arguments"]
md = "
".join(x["text"] for x in json.loads(args))
```

`tools`의 function name으로 모드 선택: `markdown_bbox`(기본, 블록별 좌표+유형 Title/Text/Table/Section-header/List-item) ·
`markdown_no_bbox`(깨끗한 마크다운 한 덩어리, 표는 LaTeX tabular) · `detection_only`(좌표·유형만, 레이아웃 분석용).

**정확도**(정답을 아는 한글 출석부 `testdoc.png`, 2회 반복): 5행 표의 **모든 셀(성명·생년월일·숫자·수료여부) 정확**,
제목·기관·장소·연락처도 정확. 2.2초. 단 **"합계"를 두 번 다 "한계"로 오독** — 특정 글자를 일관되게 틀린다.
→ **정형 표 추출에는 바로 쓸 수 있고, 서술 문장은 검수 필요.** 생성 스크립트 `make_testdoc.py`, 시험 `probe_vision.py`/`probe_vision2.py`.

## 비전·임베딩 (2026-09-05 전수)

**비전 9종 중 1개만 생존**: `meta/llama-3.2-11b-vision-instruct`(18.3s, 제목·표 행수 정확). `content` 배열의
`image_url` 형식으로 넣어야 함. 90b 버전은 180초에도 타임아웃. 나머지(phi-3-vision·neva-22b·vila·kosmos-2·
deplot·fuyu-8b·cosmos-reason2-8b) 전부 404, `ai-synthetic-video-detector`는 500.

**임베딩 7종 중 2개 생존**(둘 다 dim=2048): `nvidia/nemotron-3-embed-1b`(0.6s) ·
`nvidia/llama-nemotron-embed-vl-1b-v2`(0.8s, 멀티모달). `/v1/embeddings`에 `input_type:"query"`,
`truncate:"END"` 필요. 나머지(llama-3.2-nv-embedqa·embed-qa-4·nv-embedqa-mistral-7b-v2·arctic-embed-l·nvclip) 404.

## 7월 조사(xlsx)와 대조 — 6주 만에 73% 소멸

`NVIDIA_모델_전수조사.xlsx`(2026-07-24): 카탈로그 118개 / 사용가능 48개 / 계정비활성 56 / 확인필요 14.
그 **사용가능 48개를 2026-09-05에 전수 재호출**한 결과:

| | 개수 |
|---|---|
| 여전히 정상 | **13** (+ 503으로 튄 nano-omni 1개 = 실질 14) |
| **410 EOL** | **31** |
| 타임아웃 | 1 (llama-3.2-90b-vision) |
| 404 | 2 (flux.1-dev/schnell — 단 이건 이미지 엔드포인트라 채팅 API 404는 정상) |

**48개 → 13개(27%).** 카탈로그 자체도 118 → 81로 줄었다.
사라진 것 중엔 llama-3.3-70b·llama-3.1-70b/8b·mixtral-8x7b·gpt-oss-120b·nemotron-super-49b-v1.5(대시보드 6종)
외에 qwen3-next-80b, llama-4-maverick, mistral-small-4, deepseek-v4-pro/flash, gemma-3n, solar-10.7b,
step-3.5/3.7-flash, seed-oss-36b, minimax-m2.7 등이 포함된다.
임베딩도 nv-embed-v1·nv-embedqa-e5-v5·nv-embedcode-7b-v1·llama-nemotron-embed-1b-v2가 전부 EOL.

**교훈: 이 API는 6주 주기로 갈아엎힌다고 보고, 모델을 하드코딩 단일 지정하지 말 것.**
대시보드의 `POSTER_TEXT_MODELS`처럼 폴백 목록으로 두고, 분기마다 `compare_july.py` 방식으로 재점검할 것.

## 아직 안 한 것

1. ~~nemotron-parse 이미지 입력 시험~~ ✅ 완료
2. ~~비전/임베딩 계열~~ ✅ 완료
3. ~~xlsx 대조~~ ✅ 완료
4. 프로젝트별 활용 제안 정리 (서류 OCR / 주식 / 쇼츠 / 제안서 PPT) — 미완
5. 대시보드에 비전 모드(llama-3.2-11b-vision)·문서파서 모드(nemotron-parse) 추가 검토 — 미착수
